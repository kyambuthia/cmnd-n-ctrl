# Android Shell (Scaffold)

Kotlin shell stubs for invoking Rust core APIs via UniFFI-generated bindings.

## TODO
- Generate UniFFI bindings from Rust interface crate.
- Implement explicit consent UI before sensitive tool execution.
- Map Android special permissions (overlay/accessibility/etc.) to policy capability tiers.
