package com.example.assistant

// Stub only: replace with Android Activity + Compose/View UI.
class MainActivity {
    fun start() {
        val bridge = CoreBridge()
        println("Android shell stub started: ${bridge.ping()}")
    }
}
